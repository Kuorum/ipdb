<nav class="navbar navbar-inverse navbar-default" style="border-radius:0px;" role="navigation">
    <div class="container">
		<!-- Brand and toggle get grouped for better mobile display -->
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
			  <span class="sr-only">Toggle navigation</span>
			  <span class="icon-bar"></span>
			  <span class="icon-bar"></span>
			  <span class="icon-bar"></span>
			</button>
			<%= link_to "IPDB", root_path, :class => "navbar-brand", :id => "logo" %>
		</div>

		<!-- Collect the nav links, forms, and other content for toggling -->
		<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">	

			<% if user_signed_in? %>
				<% if current_user.role_id == 1 || current_user.role_id == 4 %>
					<ul class="nav navbar-nav">
					  <li class="dropdown">
					    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Manage <b class="caret"></b></a>
					    <ul class="dropdown-menu">
					      <% if current_user.role_id == 1 %>	
					      	<li><%= link_to "Users", users_path %></li>
					     	<li><%= link_to "Permission", permissions_path %></li>
					      <% end %>
					      <li><%= link_to "Audit changes", versions_path %></li>
					    </ul>
					  </li>
					</ul>	
				<% end %>
			<% end %>

			<ul class="nav navbar-nav navbar-right">
			  	<% if user_signed_in? %>
					<li style="margin-right:10px;">
						<div class="dropdown">
						  <button class="btn-link btn-sm dropdown-toggle" type="button" data-toggle="dropdown"><%= current_user.full_name %>
						  <span class="caret"></span></button>
						  <ul class="dropdown-menu dropdown-menu-admin">
							<li><%= link_to 'Setting', edit_user_registration_path, :method => :get %></li>
						  </ul>
						</div>
					</li>
					<li><%= button_to 'Sign out', users_sign_out_path, :class => "btn btn-sm btn-danger", :method => :get %></li>
		        <% end %>    
			</ul>

		</div><!-- /.navbar-collapse -->
	</div>  
</nav>