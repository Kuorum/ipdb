<%= form_for([@political_party.region, @political_party]) do |f| %>
  <% if @political_party.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@political_party.errors.count, "error") %> prohibited this political_party from being saved:</h2>

      <ul>
      <% @political_party.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :leaning_index %><br>
    <%= f.text_field :leaning_index %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
  
<% end %>
